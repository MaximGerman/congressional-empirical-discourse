throttle_seconds = 1.2) {
message("\n==== Congress ", cong, " ====")
offset <- "*"
out <- list()
page <- 1
repeat {
message("Congress ", cong, " | page ", page, " | offsetMark=", offset)
query <- list(
offsetMark = offset,
pageSize   = page_size,
collection = "CHRG",
docClass   = "HHRG",  # House hearings only
congress   = as.character(cong),
api_key    = api_key
)
dat <- get_page(query)
pkgs <- dat$packages %||% list()
out <- c(out, pkgs)
next_off <- dat$nextPage %||% ""
if (!nzchar(next_off)) break
offset <- next_off
page <- page + 1
# polite throttling to reduce 429s
Sys.sleep(throttle_seconds)
}
# Convert to tibble + de-dup *within congress*
df_cong <- map_dfr(out, as_row) %>%
mutate(congress = as.integer(congress)) %>%
distinct(packageId, .keep_all = TRUE)
message("Congress ", cong, ": pulled ", nrow(df_cong), " unique packageIds.")
df_cong
}
# ---- Download across congresses (105–114) ----
congresses <- 105:114
hearings_all <- map_dfr(congresses, fetch_congress) %>%
# Final global de-dup across congresses (should be rare, but safe)
distinct(packageId, .keep_all = TRUE)
library(httr)
library(purrr)
library(dplyr)
library(tibble)
library(readr)
library(stringr)
api_key <- "u9B8x7eCnRqZMd2pm0pfUSw7LDATCpveLDIzTJdN"
`%||%` <- function(x, y) if (is.null(x)) y else x
as_row <- function(x) {
tibble(
packageId    = pluck(x, "packageId", .default = NA_character_),
title        = pluck(x, "title", .default = NA_character_),
congress     = suppressWarnings(as.integer(pluck(x, "congress", .default = NA_character_))),
dateIssued   = pluck(x, "dateIssued", .default = NA_character_),
chamber      = pluck(x, "chamber", .default = NA_character_),
docClass     = pluck(x, "docClass", .default = NA_character_),
lastModified = pluck(x, "lastModified", .default = NA_character_)
)
}
# Exponential backoff + jitter; retries 429 and 5xx
get_page <- function(url, query, max_tries = 12) {
for (i in seq_len(max_tries)) {
resp <- GET(url, query = query)
sc <- status_code(resp)
if (sc == 200) {
return(content(resp, as = "parsed", type = "application/json"))
}
if (sc == 429 || (sc >= 500 && sc < 600)) {
# exponential backoff with jitter
base_wait <- min(300, 2^(i - 1))     # cap at 5 minutes
jitter <- runif(1, 0, 1)
wait <- base_wait + jitter
message("HTTP ", sc, " (try ", i, "/", max_tries, "). Sleeping ", round(wait, 1), "s then retrying...")
Sys.sleep(wait)
next
}
# non-retryable
stop_for_status(resp)
}
stop("Failed after ", max_tries, " tries. Last status: ", sc)
}
# Fetch ALL pages for a (congress, year) slice.
fetch_slice <- function(cong, year,
page_size = 1000,
throttle_seconds = 0.8) {
url <- sprintf("https://api.govinfo.gov/published/%d-01-01/%d-12-31", year, year)
offset <- "*"
out <- list()
page <- 1
repeat {
message("Congress ", cong, " | Year ", year, " | page ", page, " | offsetMark=", offset)
query <- list(
offsetMark = offset,
pageSize   = page_size,
collection = "CHRG",
docClass   = "HHRG",
congress   = as.character(cong),
api_key    = api_key
)
dat <- get_page(url, query)
pkgs <- dat$packages %||% list()
out <- c(out, pkgs)
next_off <- dat$nextPage %||% ""
if (!nzchar(next_off)) break
offset <- next_off
page <- page + 1
Sys.sleep(throttle_seconds)
}
map_dfr(out, as_row) %>%
mutate(slice_year = year) %>%
distinct(packageId, .keep_all = TRUE)
}
# ---- Driver: loop congresses and years, checkpoint each successful slice ----
out_dir <- "govinfo_house_hearings_slices"
dir.create(out_dir, showWarnings = FALSE)
congress_years <- tibble(
congress = 105:114,
# congress-year mapping for 1997–2016 (simple approximation by calendar year filter):
# We'll just try every year 1997–2016 for each congress; congress filter ensures correctness.
year = rep(1997:2016, times = length(105:114))
)
library(httr)
library(purrr)
library(dplyr)
library(tibble)
library(readr)
library(stringr)
api_key <- "u9B8x7eCnRqZMd2pm0pfUSw7LDATCpveLDIzTJdN"
`%||%` <- function(x, y) if (is.null(x)) y else x
as_row <- function(x) {
tibble(
packageId    = pluck(x, "packageId", .default = NA_character_),
title        = pluck(x, "title", .default = NA_character_),
congress     = suppressWarnings(as.integer(pluck(x, "congress", .default = NA_character_))),
dateIssued   = pluck(x, "dateIssued", .default = NA_character_),
chamber      = pluck(x, "chamber", .default = NA_character_),
docClass     = pluck(x, "docClass", .default = NA_character_),
lastModified = pluck(x, "lastModified", .default = NA_character_)
)
}
# Exponential backoff + jitter; retries 429 and 5xx
get_page <- function(url, query, max_tries = 12) {
for (i in seq_len(max_tries)) {
resp <- GET(url, query = query)
sc <- status_code(resp)
if (sc == 200) {
return(content(resp, as = "parsed", type = "application/json"))
}
if (sc == 429 || (sc >= 500 && sc < 600)) {
# exponential backoff with jitter
base_wait <- min(300, 2^(i - 1))     # cap at 5 minutes
jitter <- runif(1, 0, 1)
wait <- base_wait + jitter
message("HTTP ", sc, " (try ", i, "/", max_tries, "). Sleeping ", round(wait, 1), "s then retrying...")
Sys.sleep(wait)
next
}
# non-retryable
stop_for_status(resp)
}
stop("Failed after ", max_tries, " tries. Last status: ", sc)
}
# Fetch ALL pages for a (congress, year) slice.
fetch_slice <- function(cong, year,
page_size = 1000,
throttle_seconds = 0.8) {
url <- sprintf("https://api.govinfo.gov/published/%d-01-01/%d-12-31", year, year)
offset <- "*"
out <- list()
page <- 1
repeat {
message("Congress ", cong, " | Year ", year, " | page ", page, " | offsetMark=", offset)
query <- list(
offsetMark = offset,
pageSize   = page_size,
collection = "CHRG",
docClass   = "HHRG",
congress   = as.character(cong),
api_key    = api_key
)
dat <- get_page(url, query)
pkgs <- dat$packages %||% list()
out <- c(out, pkgs)
next_off <- dat$nextPage %||% ""
if (!nzchar(next_off)) break
offset <- next_off
page <- page + 1
Sys.sleep(throttle_seconds)
}
map_dfr(out, as_row) %>%
mutate(slice_year = year) %>%
distinct(packageId, .keep_all = TRUE)
}
# ---- Driver: loop congresses and years, checkpoint each successful slice ----
out_dir <- "govinfo_house_hearings_slices"
dir.create(out_dir, showWarnings = FALSE)
congress_years <- tibble(
congress = 105:114,
start_year = c(1997,1999,2001,2003,2005,2007,2009,2011,2013,2015),
end_year   = c(1998,2000,2002,2004,2006,2008,2010,2012,2014,2016)
) %>%
rowwise() %>%
mutate(year = list(seq(start_year, end_year))) %>%
ungroup() %>%
tidyr::unnest(year) %>%
select(congress, year)
failed <- list()
for (i in seq_len(nrow(congress_years))) {
cong <- congress_years$congress[i]
yr   <- congress_years$year[i]
outfile <- file.path(out_dir, sprintf("HHRG_cong%03d_%d.csv", cong, yr))
# Skip if already downloaded (resume-friendly)
if (file.exists(outfile)) next
res <- tryCatch(
{
df_slice <- fetch_slice(cong, yr)
# Always write, even if 0 rows (so we know it completed)
write_csv(df_slice, outfile)
message("Saved: ", outfile, " (rows=", nrow(df_slice), ")")
TRUE
},
error = function(e) {
message("FAILED slice cong=", cong, " year=", yr, " : ", conditionMessage(e))
failed[[length(failed) + 1]] <<- tibble(congress = cong, year = yr, error = conditionMessage(e))
FALSE
}
)
}
hearings_all
knitr::opts_chunk$set(echo = TRUE)
path = "C:/Users/USER/Documents/empirical_evidence_corren"
# Set the working directory for the whole document
# knitr::opts_knit$set(root.dir = path)
knitr::opts_knit$set(root.dir = here::here(path))
library(ggplot2)
library(dplyr)
library(broom)
library(ggpubr)
library(tidyr)
library(gtable)
library(grid) # for unit.pmax()
library(gridExtra)
library(lubridate)
library(stargazer)
library(vtable)
library(stringr)
library(lmtest)
library(sandwich)
library(purrr)
library("readxl")
library(here)
library(haven)
library(scales)
load(file=paste(here(),"data", "df_withempirical.RData",sep = '/'))
head(df)
install.packages("data.table")
head(df)
head(df$chamber)
library(data.table)
# big df you already have
# df <- ...  # 2,870,826 rows, has file_name like "CHRG-110hhrg34799.txt"
setDT(df)
# 1) Create packageId from file_name
# (handles .txt at end; if sometimes .pdf etc., see note below)
df[, packageId := sub("\\.txt$", "", file_name)]
# 2) Load hearings list (the CSV you saved from Python)
# Update path/name to your actual file:
hearings_path=paste(here(),"data", "govinfo_house_and_joint_hearings.csv",sep = '/')
hearings <- fread(hearings_path)
# 3) Ensure types + year exist (derive from dateIssued if needed)
# (If year already exists in the file, this will just overwrite consistently)
hearings[, year := as.integer(substr(dateIssued, 1, 4))]
# Keep only what you need + ensure key column name is packageId
keep_cols <- c("packageId", "title", "congress", "chamber", "dateIssued", "year")
hearings <- hearings[, ..keep_cols]
# Optional: remove any duplicates in hearings list by packageId (should already be unique)
setkey(hearings, packageId)
hearings <- unique(hearings, by = "packageId")
# 4) Join (left join: keep all rows in df)
setkey(df, packageId)
df_joined <- hearings[df]  # data.table syntax: x[i] keeps rows of i
# df_joined now contains original df columns + appended metadata columns
# (title, congress, chamber, dateIssued, year)
# 5) (Optional) Quick checks
cat("Rows in df:", nrow(df), "\n")
cat("Rows in df_joined:", nrow(df_joined), "\n")
cat("Matched rows (non-missing title):", sum(!is.na(df_joined$title)), "\n")
head(df_joined)
head(df$packageId)
library(data.table)
setDT(df)
# Look at a few
df[1:20, .(file_name)]
head(df$file_name)
load(file=paste(here(),"data", "df_withempirical.RData",sep = '/'))
head(df$file_name)
library(data.table)
# Keep an untouched copy for comparison
df0 <- df
# Check a few values BEFORE
head(df0$file_name, 10)
sum(is.na(df0$file_name))
# Convert WITHOUT modifying df0
dt <- as.data.table(df0)
# Check the same things AFTER
head(dt$file_name, 10)
sum(is.na(dt$file_name))
library(data.table)
# dt is already a data.table from your test
dt[, packageId := sub("\\.txt$", "", file_name)]
dt[, packageId := toupper(packageId)]  # normalize to match hearings file if needed
# Check
dt[1:10, .(file_name, packageId)]
dt[, .(n=.N, n_pkg=sum(!is.na(packageId)), pct=round(100*mean(!is.na(packageId)), 3))]
hearings <- fread(paste(here(),"data", "govinfo_house_and_joint_hearings.csv",sep = '/'))
# normalize packageId case on both sides
hearings[, packageId := toupper(packageId)]
# ensure year exists
hearings[, year := as.integer(substr(dateIssued, 1, 4))]
hearings <- unique(hearings[, .(packageId, title, congress, chamber, dateIssued, year)], by="packageId")
setkey(hearings, packageId)
setkey(dt, packageId)
out <- hearings[dt]  # left join, keeps all rows from dt
cat("Rows:", nrow(out), "\n")
cat("Matched (non-missing title):", sum(!is.na(out$title)), "\n")
cat("Unmatched:", sum(is.na(out$title)), "\n")
# If you named your joined table `out` and the pre-join df is `dt`
out[is.na(title), .(
n = .N,
n_file_name_na = sum(is.na(file_name)),
n_packageId_na = sum(is.na(packageId))
)]
out[is.na(title) & !is.na(file_name),
head(.SD, 30),
.SDcols = c("file_name", "packageId")]
# classify unmatched by whether the ID looks like HHRG/JHRG/SHRG/etc
out[is.na(title) & !is.na(packageId), .(
is_hhrg = sum(grepl("HHRG", packageId, ignore.case=TRUE)),
is_jhrg = sum(grepl("JHRG", packageId, ignore.case=TRUE)),
is_shrg = sum(grepl("SHRG", packageId, ignore.case=TRUE)),
other   = sum(!grepl("(HHRG|JHRG|SHRG)", packageId, ignore.case=TRUE))
)]
out[is.na(title) & !is.na(file_name),
head(.SD, 30),
.SDcols = c("file_name", "packageId")]
head(out)
head(out,100)
cat("Rows:", nrow(out), "\n")
cat("Matched (non-missing title):", sum(!is.na(out$title)), "\n")
cat("Unmatched:", sum(is.na(out$title)), "\n")
out %>%
filter(!is.na(title)) %>%
head(100)
write_fst(out, paste(here(),"data","out_hearings_enriched.fst",sep = '/'), compress = 100)
install.packages("fst")
library(fst)
write_fst(out, paste(here(),"data","out_hearings_enriched.fst",sep = '/'), compress = 100)
head(out)
head(out$year_hearings)
head(out$year_speech)
head(out$year)
head(out$congress)
head(out$congress_speech)
head(out$congress_hearings)
head(out$title,out$title_hearings)
head(out$title_hearings)
head(out$title_speech)
summary(out$demratio)
summary(out$year)
sum(is.na(out$year))/nrow(out)
sum(is.na(out$year))/nrow(out)*100
sum(is.na(out$title))/nrow(out)*100
summary(out$bill)
sum(is.na(out$bill))/nrow(out)*100
out %<%
filter(is.na(bill)) %>%
head(100)
out %>%
filter(is.na(bill)) %>%
head(100)
out %>%
select(bill,title,title_speech,title_hearings,year,year_hearings,year_speech,congress_speech,congress_hearings) %>%
filter(is.na(bill)) %>%
head(1000)
sum(is.na(out$majortopic))/nrow(out)*100
sum(is.na(out$majortopic2))/nrow(out)*100
sum(is.na(out$dem_dw))/nrow(out)*100
sum(is.na(out$abs_dwnom1_rs))/nrow(out)*100
sum(is.na(out$demratio))/nrow(out)*100
df <- out
unique_years <- sort(unique(na.omit(df$year)), decreasing = FALSE)
unique_congress <- sort(unique(na.omit(df$congress)), decreasing = FALSE)
custom_ticks <- unique_years[seq(1, length(unique_years), by = 2)]  # Select every 2nd year
custom_ticks <- seq(1996, 2016, by = 2)
# Define Congress sessions and their corresponding years
congress_mapping <- data.frame(
congress = unique_congress,
start_year = seq(1997, by = 2, length.out = 10),
end_year = seq(1998, by = 2, length.out = 10)
)
# Generate custom ticks for the x-axis
custom_labels <- paste0(custom_ticks, " (", df$congress[match(custom_ticks, df$year)],"th)")
party_plot <- df %>%
filter(!is.na(dem)) %>%
mutate(
dem = as.factor(if_else(dem == 1, "D", "R"))  # Replace 1 with D and 0 with R
) %>%
group_by(hearing, dem, year) %>%
summarise(mean_empirical = mean(mean_score, na.rm = TRUE), .groups = "drop") %>%
group_by(dem, year) %>%
summarise(empirical = mean(mean_empirical, na.rm = TRUE), .groups = "drop") %>%
ggplot(aes(y = empirical, x = year, group = dem, color = dem)) +
# geom_rect(
#   data = party_control,  # Use the updated party_control dataset
#   aes(
#     xmin = start_year, xmax = end_year,  # Use start and end years for ranges
#     ymin = -Inf, ymax = Inf,
#     fill = majority_party
#   ),
#   inherit.aes = FALSE,
#   alpha = 0.2  # Transparency for shading
# ) +
geom_rect(
data = party_control,
aes(
xmin = start_year,
xmax = end_year,
ymin = -Inf,
ymax = Inf,
fill = majority_party,
alpha = power_gap_scaled
),
inherit.aes = FALSE
) +
geom_point(aes(x = year + 0.5)) +  # Shift points leftward by 0.5
#  geom_smooth(aes(x = year_hearings + 0.5),se = TRUE) +  # Add SE to smooth lines
geom_line(aes(x = year + 0.5)) +  # Replaces geom_smooth
scale_x_continuous(breaks = custom_ticks) +  # Custom tick marks for years
scale_color_manual(
values = c("R" = "red", "D" = "blue"),  # Custom colors for R and D
name = "Party"  # Legend title
) +
scale_fill_manual(
values = c("D" = "blue", "R" = "red"),  # Shading for Majority party
name = "House Control"  # Legend title for shading
) +
scale_alpha_continuous(range = c(0.1, 0.5)) +
guides(alpha = "none") +
labs(
x = "Year",
y = "Empirical Score"
) +
theme_minimal()
party_control <- read.csv(paste(here(),"data", "congresS_control.csv",sep = '/'))
party_control <- party_control %>%
filter(end_year<= 2017)
party_plot <- df %>%
filter(!is.na(dem)) %>%
mutate(
dem = as.factor(if_else(dem == 1, "D", "R"))  # Replace 1 with D and 0 with R
) %>%
group_by(hearing, dem, year) %>%
summarise(mean_empirical = mean(mean_score, na.rm = TRUE), .groups = "drop") %>%
group_by(dem, year) %>%
summarise(empirical = mean(mean_empirical, na.rm = TRUE), .groups = "drop") %>%
ggplot(aes(y = empirical, x = year, group = dem, color = dem)) +
# geom_rect(
#   data = party_control,  # Use the updated party_control dataset
#   aes(
#     xmin = start_year, xmax = end_year,  # Use start and end years for ranges
#     ymin = -Inf, ymax = Inf,
#     fill = majority_party
#   ),
#   inherit.aes = FALSE,
#   alpha = 0.2  # Transparency for shading
# ) +
geom_rect(
data = party_control,
aes(
xmin = start_year,
xmax = end_year,
ymin = -Inf,
ymax = Inf,
fill = majority_party,
alpha = power_gap_scaled
),
inherit.aes = FALSE
) +
geom_point(aes(x = year + 0.5)) +  # Shift points leftward by 0.5
#  geom_smooth(aes(x = year_hearings + 0.5),se = TRUE) +  # Add SE to smooth lines
geom_line(aes(x = year + 0.5)) +  # Replaces geom_smooth
scale_x_continuous(breaks = custom_ticks) +  # Custom tick marks for years
scale_color_manual(
values = c("R" = "red", "D" = "blue"),  # Custom colors for R and D
name = "Party"  # Legend title
) +
scale_fill_manual(
values = c("D" = "blue", "R" = "red"),  # Shading for Majority party
name = "House Control"  # Legend title for shading
) +
scale_alpha_continuous(range = c(0.1, 0.5)) +
guides(alpha = "none") +
labs(
x = "Year",
y = "Empirical Score"
) +
theme_minimal()
party_plot
ggsave(
filename = paste0(path, "/plots/party_plot.png"),
plot = party_plot,
width = 8,
height = 6,
dpi = 300
)
